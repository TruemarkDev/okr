MySQL baseline test run — fluxday
Recorded: 2026-07-13
Command: DB_NAME=fluxday DB_HOST=127.0.0.1 DB_USER=root DB_PASS="" bundle exec rake test
Ruby: 3.3.11, Rails 8.0, adapter: mysql2 (adapter: mysql2, encoding: utf8)

Result:
403 runs, 987 assertions, 0 failures, 0 errors, 16 skips

Failure set (test names): NONE — full suite green on MySQL.

This means the post-migration PG failure set minus this baseline set must
also be empty, i.e. the PG suite must be fully green (0 failures, 0 errors)
for set-diff-empty discipline to hold. Any PG failure is therefore a new
failure introduced by the migration, not a pre-existing flake, and must be
fixed (not waived) before this change can be considered complete.

Skips (16) are pre-existing and environment-independent (unrelated to DB
adapter) — not part of the failure-set comparison.
